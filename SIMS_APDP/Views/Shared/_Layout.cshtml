<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] • SIMS </title>
    <link rel="icon" href="~/favicon.ico" />

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/7.3.2/mdb.min.css" rel="stylesheet">

    <style>
<<<<<<< HEAD
        :root {
            --primary: #00b894; /* Xanh lá đậm */
            --primary-light: #55efc4;
            --primary-dark: #00a085;
            --secondary: #74b9ff;
            --bg-gradient: linear-gradient(135deg, #00b894, #00cec9);
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: var(--bg-gradient);
            min-height: 100vh;
            margin: 0;
        }

        .navbar-custom {
            background: rgba(0, 184, 148, 0.95) !important;
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 20px rgba(0,0,0,0.2);
        }

            .navbar-custom .navbar-brand {
                font-weight: 800;
                font-size: 1.8rem;
                color: white !important;
            }

        .btn-primary-custom {
            background: linear-gradient(45deg, #00b894, #00cec9);
            border: none;
            border-radius: 50px;
            padding: 10px 30px;
            font-weight: 600;
        }

            .btn-primary-custom:hover {
                transform: translateY(-3px);
                box-shadow: 0 10px 25px rgba(0,184,148,0.4);
            }
    </style>
</head>
<body>

    <!-- Navbar xanh lá đẹp -->
    <nav class="navbar navbar-expand-lg navbar-dark navbar-custom shadow-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">SIMS</a>
            <div class="ms-auto">
                @if (User.Identity.IsAuthenticated)
                {
                    <span class="text-white me-3">Xin chào, @User.Identity.Name</span>
                    <a href="/Account/Logout" class="btn btn-outline-light btn-sm">Đăng xuất</a>
=======
    :root {
        --primary: #00b894;
        --bg-gradient: linear-gradient(135deg, #00b894, #00cec9);
    }
    body {
        font-family: 'Poppins', sans-serif;
        background: var(--bg-gradient);
        min-height: 100vh;
        margin: 0;
    }
    .navbar-custom {
        background: rgba(0, 184, 148, 0.98) !important;
        backdrop-filter: blur(12px);
        border-bottom: 1px solid rgba(255,255,255,0.1);
        box-shadow: 0 4px 30px rgba(0,0,0,0.3);
    }
    .navbar-custom .navbar-brand {
        font-weight: 700;
        font-size: 1.8rem;
        color: white !important;
        text-shadow: 0 2px 10px rgba(0,0,0,0.4);
        transition: all 0.3s;
    }
    .navbar-custom .navbar-brand:hover {
        transform: scale(1.05);
    }

    /* DROPDOWN HIỆN GẦN THANH NAVBAR – ĐẸP HOÀN HẢO */
    .dropdown-menu-right-fix {
           right: 0 !important;
        left: auto !important;
        top: 100% !important;           /* Bắt đầu ngay dưới navbar */
        margin-top: 2px !important;     /* Chỉ cách 2px – gần nhất có thể */
        transform: translateX(-10px) !important; /* Đẩy sang trái một chút để không bị cắt */
        min-width: 220px;
        border-radius: 12px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        border: none;
    }

    @@media (max-width: 768px) {
        .dropdown-menu-right-fix {
            right: auto !important;
            left: 50% !important;
            transform: translateX(-50%) translateY(5px) !important;
            margin-top: 5px !important;
        }
    }
</style>>
</head>
<body>

    <!-- NAVBAR + DROPDOWN ĐẸP HOÀN HẢO -->
    <nav class="navbar navbar-expand-lg navbar-dark navbar-custom shadow-lg fixed-top">
        <div class="container-fluid">

            <!-- Logo SIMS - chuyển theo Role -->
            @{
                var homeUrl = "/";
                if (User.Identity.IsAuthenticated)
                {
                    var roleIdClaim = User.FindFirst("RoleId")?.Value;
                    if (int.TryParse(roleIdClaim, out int roleId))
                    {
                        homeUrl = roleId switch
                        {
                            1 => "/Admin",
                            2 => "/Teacher",
                            3 => "/Student",
                            _ => "/"
                        };
                    }
                }
            }
            <a class="navbar-brand" href="@homeUrl">
                <i class="fas fa-home me-2"></i>SIMS
            </a>

            <!-- PHẦN BÊN PHẢI: TÊN NGƯỜI DÙNG + DROPDOWN -->
            <div class="ms-auto d-flex align-items-center gap-3">
                @if (User.Identity.IsAuthenticated)
                {
                    <div class="dropdown">
                        <a class="d-flex align-items-center text-white text-decoration-none dropdown-toggle"
                           id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false" style="cursor:pointer;">
                            <i class="fas fa-user-circle fa-2x me-2"></i>
                            <span class="fw-medium">@User.Identity.Name</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-right-fix shadow-lg border-0"
                            aria-labelledby="userDropdown">
                            <li>
                                <a class="dropdown-item" href="@Url.Action("Index", "Account")">
                                    <i class="fas fa-id-card me-2 text-success"></i>Personal Information
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item text-danger" href="/logout">
                                    <i class="fas fa-sign-out-alt me-2"></i>Đăng xuất
                                </a>
                            </li>
                        </ul>
                    </div>
>>>>>>> 774f573 (minh up role student)
                }
                else
                {
                    <a href="/Login" class="btn btn-outline-light me-2">Đăng nhập</a>
                    <a href="/Register" class="btn btn-light text-dark">Đăng ký</a>
                }
            </div>
        </div>
    </nav>

    <div style="padding-top: 90px; min-height: 100vh; display: flex; align-items: center; justify-content: center;">
        @RenderBody()
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/7.3.2/mdb.umd.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>