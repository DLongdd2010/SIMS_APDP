@{
    ViewData["Title"] = "Teaching Schedule";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model List<SIMS_APDP.Models.Timetable>

<div class="container py-5">
    <h2 class="text-center mb-5 text-info fw-bold">Teaching Timetable</h2>

    @if (!Model.Any())
    {
        <div class="alert alert-warning text-center">
            <i class="fas fa-calendar-times fa-3x mb-3"></i>
            <p>No teaching schedule has been arranged.</p>
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-bordered table-hover align-middle">
                <thead class="table-primary">
                    <tr>
                        <th>Day</th>
                        <th>Course</th>
                        <th>Time</th>
                        <th>Room</th>
                        <th>Semester</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var t in Model)
                    {
                        <tr>
                            <td class="fw-bold">@t.DayOfWeek</td>
                            <td>@t.Course?.CourseName</td>
                            <td>@t.StartTime.ToString(@"hh\:mm") - @t.EndTime.ToString(@"hh\:mm")</td>
                            <td>@(t.Room?.RoomName ?? "Not assigned")</td>
                            <td>@t.Semester</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
